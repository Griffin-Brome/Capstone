<%= form_with(model: study_time) do |form| %>
  <% if study_time.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(study_time.errors.count, "error") %> prohibited this study_time from being saved:</h2>

      <ul>
        <% study_time.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :start %>
    <%= form.datetime_select :start %>
  </div>

  <div class="field">
    <%= form.label :end %>
    <%= form.datetime_select :end %>
  </div>

  <div class="field">
    <%= form.label :deadline_id %>
    <%= form.text_field :deadline_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
